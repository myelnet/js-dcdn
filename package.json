{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install-all": "yarn install && yarn plugin import workspace-tools", 
    "build": "yarn workspaces foreach run build"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
