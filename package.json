{
  "name": "dcdn",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run --concurrency 1 test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.2.1"
  }
}
